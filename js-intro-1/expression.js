/*
    arithmetic expression is a combination of . . .
    operands(values, variables, etc.)
    operators(+ - * / %)
    that can be evaluyated to a value
    ex. y = x + 5;
*/

/*
let students = 20;
students = students + 1;
 students = students - 1;
 students = students * 2;
 students = students / 2;
let extraStudents = students % 3;

students /= 2 //augmented assignment operator

let result = (1+ 2) + (7+4);

console.log(result);
*/
